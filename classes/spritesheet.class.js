class SpriteSheet {
    src;
    spriteWidth;
    frameWidth;

    constructor(src, spriteWidth, frameWidth) {
        this.src = src;
        this.spriteWidth = spriteWidth;
        this.frameWidth = frameWidth
    }
}